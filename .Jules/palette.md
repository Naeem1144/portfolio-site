## 2024-05-23 - Form Accessibility & Feedback
**Learning:** Adding `role="alert"` and `role="status"` to dynamic form messages is crucial for screen readers to announce updates without moving focus. Also, adding `aria-hidden="true"` to required field asterisks prevents redundant announcements.
**Action:** Always verify form feedback with screen reader accessibility in mind and use semantic roles for dynamic content updates.
